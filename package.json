{
  "name": "task-memory",
  "version": "0.3.0",
  "description": "A CLI tool for managing tasks with persistent memory, designed for AI-assisted development workflows",
  "type": "module",
  "license": "MIT",
  "author": "tomo",
  "repository": {
    "type": "git",
    "url": "https://github.com/TomoTom0/task-memory.git"
  },
  "keywords": [
    "cli",
    "task",
    "todo",
    "productivity",
    "ai",
    "llm",
    "claude"
  ],
  "bin": {
    "tm": "dist/index.js"
  },
  "files": [
    "dist/index.js"
  ],
  "scripts": {
    "build:js": "bun build src/index.ts --outfile=dist/index.js --target=node && chmod +x dist/index.js",
    "build:bin": "bun build src/index.ts --outfile=dist/tm --compile",
    "build": "bun run build:js && bun run build:bin",
    "prepublishOnly": "bun run build:js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20",
    "typescript": "^5"
  }
}
